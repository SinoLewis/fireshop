version: "3"
services:
  meilisearch:
    container_name: meili-search-kuma-test
    image: getmeili/meilisearch:latest
    networks:
      uptime-kuma_default:
        ipv4_address: 172.18.0.13
    ports:
      - "7070:7070"
    # TODO: MEILI API-Key
    # environment:
    #   - <environment_var1>
  fireshop:
    container_name: fireshop-kuma-test
    image: fireshop:1.0
    networks:
      uptime-kuma_default:
        ipv4_address: 172.18.0.17
    ports:
      - "6969:6969"
  uptime:
    container_name: uptime-kuma-test
    image: louislam/uptime-kuma:1
    networks:
      uptime-kuma_default:
        ipv4_address: 172.18.0.19
    depends_on:
      - meilisearch
      - fireshop
    ports:
      - "3001:3001"
networks:
  uptime-kuma_default:
    external: true
